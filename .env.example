# ──────────────────────────────────────────────
# simvi.be — Required Environment Variables
# ──────────────────────────────────────────────

# --- LLM (required) ---
LLM_PROVIDER=gemini                     # gemini | anthropic | openai
GEMINI_API_KEY=                         # Google AI Studio key
# LLM_MODEL=gemini-2.0-flash           # override default model per provider

# --- Cost protection (recommended) ---
LLM_DAILY_TOKEN_LIMIT=2000000          # 2M tokens/day hard cap
LLM_DAILY_COST_LIMIT_USD=5.00          # $5/day hard cap (0 = unlimited)

# --- Database (required for prod) ---
DATABASE_URL=postgres://user:pass@host:5432/simvibe
# Local dev alternatives:
#   DATABASE_URL=file:./data/simvibe.db   (SQLite)
#   DATABASE_URL=memory://                (in-memory, no persistence)

# --- Landing page extraction ---
EXTRACTOR_PROVIDER=jina                 # jina | firecrawl
# JINA_API_KEY=                         # optional: jina works without key
# FIRECRAWL_API_KEY=                    # required if firecrawl
# Note: when DEMO_MODE=true, extractor is forced to `pasted` (no Jina/Firecrawl calls in demo).

# --- Application ---
NODE_ENV=development
PORT=5555
# For FE proxy process on 5556:
# API_SERVER_ORIGIN=http://localhost:5555

# --- Worker (Railway service) ---
WORKER_PORT=8080
WORKER_RUN_TIMEOUT_MS=600000            # 10 min max per run

# --- Demo mode (no API keys needed) ---
# DEMO_MODE=true

# --- nad.fun launch (optional) ---
# NAD_TOKEN_FACTORY_ADDRESS=
# NAD_CHAIN_ID=
# NAD_RPC_URL=
# NAD_LAUNCH_FEE_MON=0
# NAD_CREATE_BASE_URL=https://nad.fun/create
# NAD_DEFAULT_WEBSITE_URL=
# NAD_DEFAULT_X_URL=
# NAD_DEFAULT_TELEGRAM_URL=
# NAD_DEFAULT_IMAGE_URL=
# NAD_DEFAULT_ANTI_SNIPE=false
# NAD_DEFAULT_BUNDLED=false

# --- Chain receipt (optional, feature-flagged) ---
# ENABLE_CHAIN_RECEIPT=false
